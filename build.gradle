group 'com.softwareonpurpose'

apply plugin: 'java'

sourceCompatibility = 1.8
targetCompatibility = 1.8
version '1.2.1'

repositories {
    mavenCentral()
}

configurations {
    asciidoclet
}

dependencies {
    compile 'com.google.code.gson:gson:2.8.+'
    compile 'ch.qos.logback:logback-core:1.2.+'
    compile 'ch.qos.logback:logback-classic:1.2.+'
    compile 'org.slf4j:slf4j-api:1.7.+'
    compile 'org.apache.commons:commons-lang3:3.9+'
    testCompile 'org.testng:testng:6.14.+'
    asciidoclet 'org.asciidoctor:asciidoclet:1.5.+'
}

wrapper() {
    gradleVersion = '5.3.1'
}

jar {
    manifest {
        attributes 'Implementation-Title': 'coverage4test', 'Implementation-Version': version
    }
}

task configureJavadoc {
    doLast {
        javadoc {
            options.doclet = 'org.asciidoctor.Asciidoclet'
            options.docletpath = configurations.asciidoclet.files.toList()
        }
    }
}

javadoc {
    dependsOn configureJavadoc
}

test {
    useTestNG()
    systemProperties 'property': 'value'
}